# Remove existing remote
git remote remove origin

# Add all files and make initial commit
git add .
git commit -m "App ui"

# Add remote back
git remote add origin https://github.com/harshitbhardwaj14/HotWheels_App.git

# Push (this will create main branch on remote)
git push -u origin main